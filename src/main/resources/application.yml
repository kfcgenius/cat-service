spring:
  application:
    name: cat-service

  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092

bot:
  token: 8128054626:AAFHymkyQyLhQ7JDd9NH_fqcYKJ_Mv1l53Y

session:
  timeoutDays: 1

logging:
  level:
    org.apache.kafka: WARN
